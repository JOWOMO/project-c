mutation addCompany($name:String!, $addressLine1:String!,$postalCode:String!,$city:String!){
  updateCompany(
    company:{
      name:$name,
      addressLine1:$addressLine1,
      postalCode:$postalCode,
      city:$city
    }
  ){
    id
  }
}