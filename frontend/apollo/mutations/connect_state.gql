mutation setMatchState($id: ID!, $answer: MatchAnswer!) {
  setMatchState(answer: $answer, id: $id) {
    id
    name
    email
    firstName
    lastName
    pictureUrl
    addressLine1
    postalCode
    city
  }
}
