query demandMatches($id: ID!, $cursor: CursorInput) {
  result: matchDemand(cursor: $cursor, id: $id) {
    pageInfo {
      offset
      pageSize
      hasNextPage
    }

    matches {
      distance
      percentage
      match: demand {
        id
        name
        description
        skills {
          name
        }
        quantity
        salary: maxHourlySalary
        company {
          id
          name
          addressLine1
          postalCode
          city
          contact {
            firstName
            lastName
            pictureUrl
          }
          industry {
            name
          }
        }
      }
    }
  }
}
