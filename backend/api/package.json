{
  "name": "@beetobee/api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy --stage dev",
    "remove": "serverless remove --stage dev",
    "dev": "source env/bin/activate && PYTHONDONTWRITEBYTECODE=1 serverless wsgi serve"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "forever": "^2.0.0",
    "serverless": "*",
    "serverless-python-requirements": "^5.1.0",
    "serverless-wsgi": "^1.7.4"
  }
}
